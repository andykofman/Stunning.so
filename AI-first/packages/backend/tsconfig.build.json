{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "paths": {
      "@shared/*": ["../shared/src/*"]
    }
  },
  // Keep includes from base tsconfig (src and shared) so shared code is emitted into dist
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts"]
}